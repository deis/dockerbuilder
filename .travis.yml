language: go
go:
  - 1.5.1
branches:
  only:
    - master
services:
  - docker
sudo: required
install:
  - pip install -r rootfs/dev_requirements.txt
script:
  - make test
after_success:
  - make docker-build
